services:
  postgres:
    image: postgres:16
    container_name: secretary-pg
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: secretary
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
